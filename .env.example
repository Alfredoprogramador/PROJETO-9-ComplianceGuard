# Example values only: replace all CHANGE_ME_* entries before use.
# Use strong credentials (minimum: 16 chars, mixed case, numbers, symbols).
POSTGRES_DB=complianceguard
POSTGRES_USER=complianceguard
POSTGRES_PASSWORD=CHANGE_ME_postgres_password
REDIS_PASSWORD=CHANGE_ME_redis_password
MONGO_INITDB_ROOT_USERNAME=complianceguard
MONGO_INITDB_ROOT_PASSWORD=CHANGE_ME_mongo_password
